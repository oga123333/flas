# 🔧 Flask Utility

A lightweight Flask web tool with integrated backend processing capabilities.

## ✨ Features

- Handles requests asynchronously
- Built‑in helper binaries for common transformations
- Auto‑installs required runtime dependencies (static build)
- Extensible via environment variables
- Clean web interface with progress feedback

## 🚀 Quick Start

### 1. Clone
```bash
git clone https://github.com/yourusername/your-repo.git
cd your-repo
